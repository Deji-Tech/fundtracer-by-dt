import React from 'react';

// Placeholder component for /app-solana
export default function AppSolana() {
  return (
    <div style={{
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      height: '100vh',
      background: '#111',
      color: '#fff',
      textAlign: 'center'
    }}>
      <h1>Solana Page Placeholder</h1>
      <p>This page still needs implementation.</p>
    </div>
  );
}